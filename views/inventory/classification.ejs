<!-- This view will consist of three distinct sections: A content heading, a message, and the inventory items display.  -->
<% if (title) { %>
<h1 class="page-title"><%= title %></h1>
<% } else { 
    res.redirect("/");
} %>

<!-- comment mark (#), disables this code -->
<%# (message) %>

<%- grid %>
